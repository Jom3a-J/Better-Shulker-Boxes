# neoforge

NeoForge-specific source set for the future multiloader build.

This will hold NeoForge `@Mod` entrypoints, payload registration, client payload handlers, key mapping events, tooltip component factory event, config screen extension point, and `META-INF/neoforge.mods.toml`.
